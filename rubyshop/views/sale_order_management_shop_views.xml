<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <data noupdate="0">
        <!-- model.name action window -->
        <record id="sale_order_management_shop_action" model="ir.actions.act_window">
            <field name="name">Sale Order Management Shop</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">sale.order.management.shop</field>
            <field name="view_mode">tree,form</field>
            <field name="view_type">form</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                    <!-- Add Text Here -->
                </p><p>
                    <!-- More details about what a user can do with this object will be OK --> 
                </p>
            </field>
        </record>
        
        <!-- model.name form view -->
        <record id="sale_order_management_shop_view_form" model="ir.ui.view">
            <field name="name">Sale Order Management Shop</field>
            <field name="model">sale.order.management.shop</field>
            <field name="arch" type="xml">
                <form string="">
                    <sheet>
                        <group>
                            <field name="name"/>
                            <field name="code"/>                            
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- model.name tree view -->
        <record id="sale_order_management_shop_view_tree" model="ir.ui.view">
            <field name="name">Sale Order Management Shop</field>
            <field name="model">sale.order.management.shop</field>
            <field name="arch" type="xml">
                <tree>
                    <!-- Add your fields here -->
                     <field name="name"/>
                    <field name="code"/> 
                </tree>
            </field>
        </record>

    </data>
</odoo>